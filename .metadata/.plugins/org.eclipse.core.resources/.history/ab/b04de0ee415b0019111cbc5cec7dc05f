package com.ci.game.loot.lootsystem;

public class LootSystem 
{
	public static boolean calcNoDrop(int mLvl)
	{
		boolean isNoDrop = false;
		int noDropValue = 100;
		double probSum;
		int newNoDrop = (int) (probSum/(1/(noDropValue/(noDropValue+probSum))-1));
		return isNoDrop;
	}
}
